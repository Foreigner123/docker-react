sudo: required
services:
  - docker

before_install:
  - sudo docker build - t foreigner123/docker-react -f Dockerfile.dev .

script:
  - docker run foreigner123/docker-react npm run test -- --coverage
